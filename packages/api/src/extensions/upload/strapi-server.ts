/**
 * Upload plugin extension placeholder
 * Currently not needed as we use Strapi's built-in folder system
 * for organizing files in the media library
 */

export default (plugin: any) => {
  // No modifications needed - Strapi's folder system handles organization
  return plugin
}
